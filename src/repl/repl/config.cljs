(ns repl.repl.config)

(comment
  ;; Local
  (defonce server-host "localhost:56665")

  ;; Heroku
  (defonce server-host "repl-repl.herokuapp.com")
  )

(defonce server-host "repl-repl.herokuapp.com")


